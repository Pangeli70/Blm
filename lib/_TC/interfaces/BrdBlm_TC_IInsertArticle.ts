/**
 * Caratteristiche degli inserti per la chiusura tecnica per il pannello o per l'anta
 * ad esempio il colore della cornice dell'oblò per ogni
 * singolo articolo dell'anagrafica
 */



export interface BrdBlm_TC_IInsertArticle {
  // TODO verificare nome
  code: string;
  extColor?: number;
  intColor?: number;
}
